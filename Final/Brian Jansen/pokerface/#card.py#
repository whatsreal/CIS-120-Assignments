
class card(object):
	def __init__(self, suit, name, numval, numid):
		self.suit = suit
		self.name = name
		self.numval = numval
		self.numid = numid
		if suit is "s" or suit is "c":
			self.color = "b"
		else:
			self.color = "r"		
		
	
